# Spec Delta: workbench (ISSUE-440)

## Scope

执行 `openspec/changes/workbench-p5-04-command-palette` 的交付闭环，补齐命令面板分类搜索、文件打开、空结果文案、分页与最近使用记录。

## Acceptance

- `openspec/changes/workbench-p5-04-command-palette/tasks.md` 六段任务全部勾选完成。
- `CommandPalette` 与 `AppShell` 行为满足 `workbench-delta.md` 新增/修改场景。
- `openspec/_ops/task_runs/ISSUE-440.md` 记录关键命令与 Red/Green 证据。
